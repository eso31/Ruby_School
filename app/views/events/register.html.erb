<h1>Register to <%= @event.name %> </h1>
<table>
  <thead>
    <tr>
      <th>Email</th>
    </tr>
  </thead>
  <tbody>
    <% @users_of_event.each do |u| %>
      <tr>
        <td> <%=u.email%> </td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= @hola %>

<%= form_tag controller: "events", action: "register_user", method: :post do %>
  <%= label_tag "Email" %>
  <%= text_field_tag :email %>

  <%= submit_tag "Register user" %>
<% end %>
